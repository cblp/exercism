name: rail-fence-cipher
version: 1.1.0.4

dependencies:
  - base
language: GHC2021

library:
  exposed-modules: RailFenceCipher
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - vector

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - rail-fence-cipher
      - hspec
