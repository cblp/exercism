name: poker
version: 1.1.0.2

language: GHC2021

dependencies:
  - base

library:
  exposed-modules: Poker
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - poker
      - hspec
