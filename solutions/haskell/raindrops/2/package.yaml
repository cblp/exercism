name: raindrops
version: 1.1.0.6

dependencies:
  - base

library:
  exposed-modules: Raindrops
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - text

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - raindrops
      - hspec
