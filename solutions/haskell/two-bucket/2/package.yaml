name: two-bucket
version: 1.0.0.0

language: GHC2021
dependencies:
  - base

library:
  exposed-modules: TwoBucket
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - containers
    - generic-lens
    - lens
    - mtl

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - two-bucket
      - hspec
