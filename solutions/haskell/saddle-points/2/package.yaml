name: saddle-points
version: 1.5.0.8

dependencies:
  - array
  - base

language: GHC2021

library:
  exposed-modules: Matrix
  source-dirs: src
  ghc-options: -Wall
  # dependencies:
  #   - mtl

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - saddle-points
      - hspec
