name: atbash-cipher
version: 1.2.0.7

language: GHC2021
dependencies:
  - base

library:
  exposed-modules: Atbash
  source-dirs: src
  ghc-options: -Wall
  # dependencies:
  #   - extra

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - atbash-cipher
      - hspec
