name: change
version: 1.3.0.7

dependencies:
  - base

language: GHC2021

library:
  exposed-modules: Change
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - change
      - hspec
